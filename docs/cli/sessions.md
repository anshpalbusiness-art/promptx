---
summary: "CLI reference for `promptx sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `promptx sessions`

List stored conversation sessions.

```bash
promptx sessions
promptx sessions --active 120
promptx sessions --json
```
