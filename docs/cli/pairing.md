---
summary: "CLI reference for `promptx pairing` (approve/list pairing requests)"
read_when:
  - You’re using pairing-mode DMs and need to approve senders
title: "pairing"
---

# `promptx pairing`

Approve or inspect DM pairing requests (for channels that support pairing).

Related:

- Pairing flow: [Pairing](/channels/pairing)

## Commands

```bash
promptx pairing list whatsapp
promptx pairing approve whatsapp <code> --notify
```
