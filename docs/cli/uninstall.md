---
summary: "CLI reference for `promptx uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `promptx uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
promptx uninstall
promptx uninstall --all --yes
promptx uninstall --dry-run
```
